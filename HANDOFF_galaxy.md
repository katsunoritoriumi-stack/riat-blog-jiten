# 引き継ぎ書: RIATブログ・ギャラクシー（galaxy）

最終更新: 2026-08-13（**質問機能を Render から Vercel 関数へ移設**。galaxy を git にコミット済み。残タスクなし）

## 1. これは何か

RIATブログ（seimeiron.com）全407記事を Three.js の3D銀河として可視化した静的サイト。
記事＝星、意味的な近さ＝空間的な近さ、テーマ星団＝AIが命名した12星座。
星をクリックすると右パネルが開き、**記事 / クイズ / 質問** の3タブで深掘りできる。

- 本番URL: **https://galaxy-wheat-zeta.vercel.app** （公開・認証なし）
- ソース: `C:\Users\katsu\RIATブログ辞典\galaxy\`（RIATリポ内サブフォルダ、Exodus と同パターン）
- 技術: 素の HTML + Three.js 0.160（unpkg CDN / importmap）＋ Vercel サーバーレス関数1本
- ランニングコスト: 0円（クイズと要約は静的同梱。質問だけ Gemini 無料枠を使う）

> **2026-08-13、質問機能（`/ask`）を Render から Vercel の関数へ移した。**
> Render の無料インスタンスがスピンダウンからの復帰に失敗して無応答になる事故が2日続いたため
> （§5 の罠を参照）。いまギャラクシーは **Render を一切使っていない**。
> `server.py` の `/ask` は辞典サイト本体用にそのまま残してあるが、銀河からは呼んでいない。

## 2. 起動・デプロイ

- ローカル起動: `galaxy/` で **`npx vercel dev --listen 5900`**
  - 関数を動かすので preview ツールの "galaxy"（静的配信）では `/api/ask` が 404 になる。
    見た目だけ見るなら preview でよい
  - **環境変数は `galaxy/.env.local`**（`.gitignore` 済み）。`vercel dev` がこれを読まないことが
    あったので、起動シェルに `$env:GENAI_API_KEY` などを入れてから起動するのが確実
- 本番デプロイ: `galaxy/` フォルダで `npx vercel deploy --prod --yes`
  - プロジェクト名 `galaxy`、スコープ `katsunoritoriumi-2409s-projects`
  - 確認は **https://galaxy-wheat-zeta.vercel.app**（エイリアス）を実際に開く。
    ハッシュ付きURL（galaxy-xxxx-...）は不変スナップショットなので確認に使わない
  - **プレビューURL（`--prod` なし）は Vercel の SSO 保護が掛かる**ので、外から API を叩けない。
    実機で人が見る用途にしか使えない
- データ再生成:
  - 星の座標・要約 … `venv/Scripts/python.exe galaxy/tools/build_galaxy.py`
  - クイズ紐付け … `python galaxy/tools/build_quiz_link.py`
  - 関数へ同梱する記事全文 … `python galaxy/tools/build_articles.py`（記事を増やしたら必ず回す）

## 3. ファイル構成

| ファイル | 役割 |
|---|---|
| `galaxy/index.html` | 銀河本体（描画・操作・パネル・検索・クイズ・質問、1ファイル完結 約1200行） |
| `galaxy/api/ask.mjs` | **質問機能の本体**（Vercel 関数）。Gemini と Pinecone を REST で叩く。依存パッケージ0 |
| `galaxy/api/_data/articles.json.gz` | 記事全文（407件、gzip 2.74MB）。関数が URL 引きで文脈に使う |
| `galaxy/vercel.json` | 関数の `maxDuration` と `includeFiles` の指定 |
| `galaxy/galaxy_data.json` | 生成済みデータ（407星の座標・要約・12星座名） |
| `galaxy/quiz_by_star.json` | クイズ紐付けデータ（1167問＋記事URL索引＋星座索引、約1MB） |
| `galaxy/summaries.json` | 事前生成した記事要約 |
| `galaxy/tools/build_galaxy.py` | 星データ生成バッチ（Pinecone→KMeans→対数螺旋→Gemini要約） |
| `galaxy/tools/build_quiz_link.py` | クイズ紐付け生成バッチ（quiz_pool.json を読むだけ） |
| `galaxy/tools/build_articles.py` | 記事全文を gzip で書き出すバッチ（`blog_data.json` を読むだけ） |
| `galaxy/tools/summaries_cache.json` | 要約キャッシュ。**消さない**（消すと再生成でAPI 40回消費） |
| `server.py`（リポルート） | 辞典サイト本体のバックエンド。**銀河からはもう呼んでいない** |
| **`blog_data.json`（リポルート）** | **実データ。読み取り専用、絶対に改変しない**（鉄則5） |
| **`riat-quiz-frontend/public/quiz_pool.json`** | **実データ1167問。読み取り専用**（別リポ、Basic認証で非公開のまま） |

## 4. コア仕様

### 銀河そのもの
- **座標は対数螺旋（渦巻き銀河）で決定**（build_galaxy.py の `assign_spiral_positions`）。
  KMeansクラスタを5本の腕に均等配分し、各クラスタは自分の腕の中で連続した半径帯を持つ。
  腕をまたいで飛び飛びに配置すると「リング状の塊」に見えるので、必ず同一アームで連続割当てにすること
- 星のサイズ＝記事の文字数、色＝所属星団（12色パレット）。
  点の大きさは 120px で頭打ち（`minDistance`=1.6 まで寄れるので、無いと画面を覆う）
- **カメラ操作**: `minDistance`=1.6／星をタップすると距離15まで寄る。
  **スマホは2本指を置いた瞬間に、指の中点が指す先へ注視点を移す**（`aimAt`）。
  OrbitControls のピンチは常に注視点へ寄るため、これが無いと奥の星に近づけず銀河中心に吸われる。
  three の `zoomToCursor` はホイール専用でピンチには効かないので自前。PC の挙動は変えていない
- **星ごとのタイトル札**（`updateStarLabels`）。設計理由をまとめて書いておく:
  - 札の左下を星に接させ（`sprite.center`）、画面上で必ず星から右上へ伸びる。両端に星団色のドット。
    真上に浮かせる方式だと、どの札がどの星のものか読み取れなかった
  - **画面上の高さを一定に保つ**（`LABEL_PX` = スマホ22 / PC24）。世界座標固定だと遠い星の札が潰れる。
    文字の高さは札の約7割なので、ここを14pxにすると実機で読めない
  - **重なる札は画面上の矩形判定で間引く**。最大 `MAX_LABELS`=16 枚
  - **画面外の札は表示枠を消費させない**（させると見えない札に枠を取られ、画面内に1枚しか出ない）
  - 右端からはみ出す札は星の左側へ折り返す
  - 全体表示ではカメラ距離52以内のみ。**絞り込み中は距離で切らず**、その星座の札だけを出す
- **札そのものがタップ対象**で、しかも星より優先する。読める札は「押せばこの記事」と分かる
  明示的な目印なので、近くの別の星に横取りされると押した記事と違うものが開く（実際に踏んだ）
- **銀河に浮かぶ星座名もタップ対象**。押すとその星座へ飛んで絞り込む（`goToCluster`）。
  星座を選んでいる間は、**他の星座名と星雲グローは完全に非表示**（薄く残すと選んだ星座の文字に
  重なって読めない）。非表示の星座名はタップ対象からも外す
- 待機中はゆっくり自転。**ホバー中・パネル表示中・絞り込み中は自転停止**

### パネル（星をクリック）
- **スマホ幅では全画面**（横に銀河が覗くと読みづらいとの指摘）。PCは右側430pxの側面パネルのまま
- **記事タブ** … 星座名・記事番号・タイトル・要約・「原文を読む」
- **クイズタブ** …（解説に出典は出さない。ユーザー指示で2026-08-04に削除） 初回だけ `quiz_by_star.json` を遅延fetch。
  出題元は「**記事に直結する問題 → 無ければ同じ星座の問題**」（`quizPool.js` の `candidatesFor` と同じ方針）。
  直接ヒットは **407星中128星**、残りは星座フォールバック（全12星座に在庫あることを確認済み）。
  1回5問・即時正誤＋解説・最後にスコア
- **この記事の要約タブ** … タブを開くと自動で要約を生成する。
  **出力書式は `SUMMARY_PROMPT` で固定**（「要約」2〜3文 →「要点」の・箇条書き4つ程度。
  前置き・挨拶・感想・出典・締めの言葉は書かせない）。記事ごとに体裁がばらつくと読みにくいため。
  保険としてクライアントの `plain()` が `*` `-` `•` を「・」に寄せ、`**` と見出し記号を落とす。
  `{message, url, only:true}` を投げ、**サーバーはベクトル検索をせずその記事だけで答える**。
  一度出した要約は `summaryCache` に持ち、開き直しても再取得しない（「もう一度読み解く」で再生成）。
  タブを開いた瞬間に `/warmup` を投げてコールドスタートを緩和。
  ※2026-08-04 に「質問（自由入力のRAGチャット）」から作り替えた。自由質問は HUD の質問窓へ移した

### 質問窓・星座フォーカス（2026-08-04 本番投入済）
- HUD右上は**ブログ辞典と同じ問答窓**。質問を入れて Enter で全画面の回答ページ（`#askpage`）が開き、
  ページ末尾に**出典ブログ記事への外部リンク**が並ぶ。
  「別ページ」は同一アプリ内の全画面表示。別URLに飛ばすと銀河を読み込み直すため
  ※当初はキーワードで星を絞り込む検索窓だったが、2026-08-04 に問答形式へ置き換えた（検索は廃止）
- 凡例チップは**トグル式のフォーカス**。押すとその星座だけ残して星座の広がりに応じた距離で寄り、
  もう一度押すと全体に戻る。検索は星座フォーカスより優先される。Esc で全解除
- **絞り込み中は対象外の星をレイキャストからも除外**（手前に被っていても狙った星を掴める）。
  当たり判定の半径はカメラ距離に比例（`camDist * 0.03` を 1.4〜4.5 でクランプ）。
  タッチ環境（`pointer: coarse`）では半径2倍・タップ許容の揺れ12px
- **何もない所をタップすると段階的に戻る**: 星の上＝記事を開く／パネルが開いていれば閉じるだけ／
  それも無ければ絞り込み解除＋俯瞰（原点・距離205、向きは現在の角度のまま）へ `flyTo`。
  1タップで両方やらないのは、パネルを読んでいる最中の誤操作を防ぐため。
  気づけないと解除できないので、絞り込み中だけ `#backhint` の案内を出す
- スマホ幅では星座チップを画面最下部の横スクロール帯にして視界を空ける。
  **`touch-action: pan-x` と `overscroll-behavior: contain` が必須**。無いとスクロールが親へ連鎖して
  「横スワイプすると画面ごと動き、チップの続きを出せない」状態になる（実機で発生）。
  `html, body` 側にも `position: fixed` + `overscroll-behavior: none` を入れてある

### 公開まわり
- **公開設定はユーザーの明示指示**（2026-07-17）。当初計画は Basic 認証付きだったが
  「パスワードなしで普通に入れる設定にしたい」との指示で `middleware.js` を削除して公開した。
  ※ブログ辞典・クイズの非公開ポリシーとは別扱い
- `<meta name="robots" content="noindex">` は残してある
- HUD右上に**全国龍管マップ**（https://dragon-shrine-checker.vercel.app/）への導線。
  この導線のために**龍管マップの Basic 認証を 2026-08-03 に解除して公開した**（ユーザー指示）

### 質問機能のバックエンド（`galaxy/api/ask.mjs`、2026-08-13〜）
- 流れは `server.py` の `ask()` と同じ: 埋め込み（gemini-embedding-001, 768次元）→
  Pinecone 検索（TOP_K=3, cosine）→ Gemini（gemini-3.1-flash-lite）をストリーミング。
  SSE のイベント形式（`sources` / `delta` / `done` / `error`）も据え置きなので、
  フロントの `streamAsk` はエンドポイントの向き先を変えただけ
- `only:true` を渡すと埋め込みと検索を丸ごと省き、`url` の記事だけで答える（要約タブのフォールバック用）
- **Pinecone のホストは URL 直書き**（`seimeiron-blog-hcmvlzp.svc.aped-4627-b74a.pinecone.io`）。
  SDK を入れずに済ませるため。インデックスを作り直したらここも直すこと
- 記事全文は `api/_data/articles.json.gz` を**コールドスタート時に1回だけ展開**して使い回す。
  `api/` 配下は Vercel が静的配信しないので、8MB の全文が公開URLで落とせる状態にはならない
  （実際に `/api/_data/...` が 404 になることを確認済み）
- 防御は **Origin チェックのみ**。サーバーレスはプロセスが使い捨てなので、`server.py` にあった
  「IP別20回/10分・全体300回/日」は移していない。実効的な上限は Gemini 無料枠 RPD 500。
  枯渇が気になったら Upstash Redis 無料枠（fetch だけで使える）で復元できる
- 上流呼び出しには必ずタイムアウトを入れてある（埋め込み・検索 20秒／生成 45秒）。
  Render の一件と同じ「返らないまま抱え込む」を作らないため
- 実測: コールドスタートでも出典 0.9秒・初回テキスト 2.4秒・完了 5.3秒

### server.py 側（`/ask` の公開・辞典サイト本体用に残置）
- `PUBLIC_PATHS = {"/warmup", "/ask"}` で Basic 認証を免除。**それ以外のルートは 401 のまま**
- `only=true` を渡すと埋め込みと Pinecone 検索を丸ごと省き、`url` の記事だけで答える（要約タブ用）
- **galaxy の Vercel プレビューURLも許可オリジン**（`PREVIEW_ORIGIN_RE`、スコープ名まで固定）。
  無いと、本番へ出す前の実機確認でプレビューが 403 になる
- 裸で開けると Gemini 無料枠（ブログ辞典本体と共用）を枯らされるので、依存を増やさず3段で絞る:
  1. **Origin チェック**（`ALLOWED_ORIGINS`）→ 不許可は 403。
     ただし非ブラウザからは詐称できるので**これは埋め込み防止であって認証ではない**
  2. **IPごとのレート制限** 20回/10分 → 429
  3. **全体の日次上限** 300回/日 → 429
- リクエストに `url` があれば、その記事を `url_to_article` から引いて**文脈と出典の先頭に固定**し、
  プロンプトで「この記事」が何を指すかを明示する

## 5. 既知の罠（必読）

- **自転とワールド座標**: シーンが自転するため、カメラのワープ先は必ず
  `scene.localToWorld()` で変換する。ローカル座標のまま `flyTo` するとズレた場所に飛ぶ
- **`hidden` 属性は `display` 指定に負ける**: `#q-result` に `display:flex` を書いたら
  `hidden` が効かず、次の問題の裏に前回のスコアが出続けた。`#q-result[hidden]{display:none}` で解決
- **RAGはその記事を引くとは限らない**: `/ask` は埋め込み検索の TOP_K=3 なので、
  星の記事自体が文脈に入らず「その記事は含まれていません」と回答する事故が起きた。
  → `url` を渡して先頭固定する現仕様で解決済み。**この挙動を消さないこと**
- **Vercel の関数で Web 標準シグネチャを使うには、HTTP メソッド名でエクスポートする**。
  `export default async function handler(request)` にすると Node の `(req, res)` 形式で呼ばれ、
  `request.headers.get is not a function` で落ちる。`export async function POST(request)` が正解
- **Gemini の SSE は CRLF 区切り**（`\r\n\r\n`）。`"\n\n"` で split すると1片も取れず、
  出典だけ出て本文が空という症状になる（実際に踏んだ）。LF に寄せてから split すること。
  末尾のイベントは区切りなしで終わることがあるので、残りバッファも必ず処理する
- **PowerShell から `vercel env add` に値をパイプすると改行が混ざる**。保存された鍵の末尾に
  改行が付き、Gemini が `API key not valid` を返す（本番で1回踏んだ）。
  Bash の `printf '%s'` で渡すか、コード側で `.trim()` すること（現在は両方やってある）
- **Render のインスタンスが「live なのに応答しない」ことがある**（2026-08-05 に約50分停止。
  2026-08-12・13 にも再発し、これが Vercel 移設の直接の理由になった）。
  **再発時の切り分け手順**（外から確定できる）:
  1. 別の Render ホストと比べる。Suspend 済みなら 0.3秒で `x-render-routing: suspend-by-user`、
     存在しないホストなら `no-server`。**対象だけ無応答ならエッジは正常＝インスタンス側の異常**
  2. ローカルで `python server.py` を動かして同じ質問を投げる。通ればコードと API 鍵は無実
  3. https://status.render.com で全体障害でないことを確認
  4. 対処は再デプロイのみ。main へ push するか、ダッシュボードの Manual Deploy
  2026-08-13 は 20分以上リクエストを送り続けても一度も応答せず、再デプロイ後は 0.2秒で復旧した。
  **一晩アイドル（スピンダウン）した翌日に死ぬ**のがパターン。

  症状: TCP接続は数十ミリ秒で成功するのに HTTP 応答が返らない（4分待っても無応答）。
  ログには `Your service is live` の後に `No open HTTP ports detected on 0.0.0.0` が並び、
  `Port scan timeout reached` で終わる。Events は緑の **Deploy live** のままなので気づきにくい。
  **対処: ダッシュボードの Manual Deploy → Clear build cache & deploy**。これで復旧した。
  このとき無料枠は 4.97/750 時間で枯渇しておらず、`server.py` も正常動作時から無変更だった。
  つまり**コードでも枠でもなく、インスタンス側の異常**。原因を code に求めて時間を溶かさないこと
- **Render のデプロイ完了を `/warmup` で判定しない**: 旧コードでも 200 を返すので、反映前に
  「完了」と誤判定する（実際に踏んだ）。新コードでしか変わらない応答（例: オリジン判定が
  403→400 に変わる）を条件にすること
- **パネル内タブの中身は `flex: none` にする**: 縦フレックスで縮められると、あふれた本文が
  下のボタンや注記に重なって表示される（実機で発生）
- **`/ask` をポーリングに使わない**: Render のデプロイ待ちに `/ask` を15秒おきに叩いて、
  自分で作ったレート制限に自分で引っかかった（Gemini も十数回無駄打ち）。
  デプロイ待ちの疎通確認は `/warmup` を使うこと
- **タッチには `pointermove` が来ない**: ホバーで求めた `hovered` に頼ると、スマホでは星をタップしても
  何も開かない（実際に踏んだ）。**タップは pointerup の位置でその場でレイキャストする**こと
- **Browserペインが閉じていると検証不能**: 非表示タブはフレーム合成せず rAF が止まるため、
  スクリーンショットが撮れず `flyTo` も `uReveal` も進まない。
  この状態だと**当たり判定が全域で外れる**ので、コードのバグと誤診しやすい（実際に誤診した）。
  疑ったら `canvas.width` と `innerWidth * devicePixelRatio` が一致するか見る。
  **タッチ周りの検証は擬似イベントより実機が確実** → `npx vercel deploy --yes`（`--prod` なし）で
  プレビューURLを出してユーザーに実機で見てもらい、OKなら `--prod` で昇格させる運用が有効だった。
  ※プレビューURLは Vercel のログインを求められる（ユーザー自身のアカウントで通る）
- **ポート5800は使用不可**（ai-atlas が使用中）。galaxy は 5900
- Vercel の環境変数 `BASIC_AUTH_PASS` は galaxy には**未設定**（公開運用のため不要）
- umap-learn / scikit-learn は **venv にのみ**インストール済み。`requirements.txt` には
  意図的に追加していない（Render の Flask デプロイを重くしないため）
- `galaxy/` は 2026-08-13 に git へコミット済み（`06b1556` で追加、`59ca8b5` で関数移設）。
  ただし**本番反映は Vercel CLI 直デプロイ**であって push では起きない。push で動くのは Render だけ

## 6. 未解決・今後の候補

### 直近の状態（2026-08-13 時点）

**急ぎの残タスクは無い。** 本日やったこと（すべて本番確認済み）:
- Render 無応答の切り分け → 再デプロイで復旧（`/warmup` 0.2秒、質問 2.8秒）
- 回答待ちの文言を「観測所と交信中…」→「**ソースと交信中…**」に変更（ユーザー指示）
- 質問機能を **Vercel 関数へ移設**し、Render 依存を解消。本番で6ケース検証済み
  （通常質問／`only:true`／空メッセージ400／501文字400／別Origin403／許可Origin200）
- 実 UI でも「TABエンジンとは？」が回答＋出典3件で通ることを確認

### 次に見ると良いこと
- **翌日以降、Render を触らないまま質問が通るか**。通れば移設の効果が確定する
- 文言の不整合: 進捗表示は「ソースと交信中…」だが、失敗時は「**観測所**が応答しません」のまま。
  そろえるかはユーザー判断（今回は指示された箇所だけ変えた）

### 判断待ち・今後の候補
- **辞典サイト本体（riat-blog-jiten-2.onrender.com）も Vercel へ移すか。**
  中身は「静的 index.html ＋ `/ask` ＋ `/blog_data.json` ＋ `/warmup`」だけなので移設は容易。
  Basic 認証は Vercel の Edge Middleware で20行ほど。やれば Render を完全に使わなくなり、
  750h/月の枠も無関係になる。銀河の移設が安定したら判断する
- 質問の流量制限（Upstash Redis 無料枠で復元できる）。いまは Origin チェックのみ
- **セキュリティ**: `C:\Users\katsu\Downloads\dragon-shrine-checker` の `.git/config` に
  GitHub トークン（`gho_...`）が平文で入っている。失効＋Git Credential Manager への移行を推奨
- Phase 3 候補: 質問→関連星へワープ（要埋め込みAPI）、記事番号順の時間リプレイ、環境音
- モバイル実機での操作感は未確認
