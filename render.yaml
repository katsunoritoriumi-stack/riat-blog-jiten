services:
  - type: web
    name: riat-blog-jiten
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: GENAI_API_KEY
        sync: false
      - key: PINECONE_API_KEY
        sync: false
